# Umbrella Chart for HashiCorp Vault
# This pattern: your repo wraps upstream Helm chart with your values
apiVersion: v2
name: vault
description: HashiCorp Vault deployment via GitOps
type: application
version: 1.0.0
appVersion: "1.15.0"

dependencies:
  - name: vault
    version: 0.28.0
    repository: https://helm.releases.hashicorp.com
